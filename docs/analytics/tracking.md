# 📊 Google Analytics 4 埋点事件清单

## 概述

本文档记录了演唱会行程一键生成器网站中所有 GA4 埋点事件，方便后续维护和数据分析。

**GA4 衡量 ID**: `G-RMGX1QY47L`

---

## 🎯 核心转化漏斗事件

### 1. `trip_generate` - 行程生成成功
**触发时机**: 用户点击"生成行程"按钮并成功获取结果后

**参数**:
```javascript
{
  depart_city: '北京',              // 出发城市
  arrival_city: '上海',             // 目的地城市
  concert_date: '2026-08-01',       // 演唱会日期
  artist_name: '周杰伦',            // 艺人名称（未填写则为"未填写"）
  venue_name: '鸟巢',               // 场馆名称
  has_flights: true,                // 是否有机票结果
  has_hotels: true,                 // 是否有酒店结果
  flights_count: 15,                // 机票结果数量
  trains_count: 8,                  // 高铁结果数量
  buses_count: 5,                   // 巴士结果数量
  hotels_count: 50                  // 酒店结果数量
}
```

**用途**: 
- 统计行程生成成功率
- 分析热门城市路线
- 追踪各艺人的搜索热度

---

### 2. `select_item` - 选择商品
**触发时机**: 用户点击"选择"按钮选择机票/高铁/巴士/酒店时

**参数**:
```javascript
{
  item_type: 'flight',              // 商品类型: flight/train/bus/hotel
  item_name: 'CA1234',              // 商品名称（航班号/车次/酒店名）
  price: 580,                       // 价格
  artist_name: '周杰伦',            // 艺人名称
  depart_city: '北京',              // 出发城市（交通类商品）
  arrival_city: '上海',             // 到达城市（交通类商品）
  venue_city: '上海',               // 场馆城市（酒店类商品）
  distance_km: 2.5                  // 距离场馆距离（酒店类商品）
}
```

**用途**:
- 分析用户对不同交通方式的偏好
- 统计各类商品的选择率
- 追踪价格敏感度

---

### 3. `begin_checkout` - 开始购买流程
**触发时机**: 用户点击"购买"/"预订"按钮时

**参数**:
```javascript
{
  item_type: 'flight',              // 商品类型
  artist_name: '周杰伦',            // 艺人名称
  depart_city: '北京',              // 出发城市
  arrival_city: '上海',             // 到达城市
  concert_date: '2026-08-01'        // 演唱会日期
}
```

**用途**:
- 追踪购买意向
- 计算转化漏斗中的流失率

---

### 4. `purchase` - 订单创建成功（核心转化指标）
**触发时机**: 用户填写完信息，成功创建订单后

**参数**:
```javascript
{
  transaction_id: 'ORD123456',      // 订单ID
  value: 580,                       // 订单金额
  currency: 'CNY',                  // 货币
  item_type: 'flight',              // 商品类型
  item_name: 'CA1234 北京→上海',    // 商品名称
  artist_name: '周杰伦',            // 艺人名称
  concert_date: '2026-08-01',       // 演唱会日期
  depart_city: '北京',              // 出发城市
  arrival_city: '上海'              // 到达城市
}
```

**用途**:
- 核心转化指标，统计成交订单
- 按艺人统计订单转化率
- 计算GMV（成交总额）
- 分析不同商品类型的转化效果

---

## 🎤 艺人热度追踪事件

### 5. `search` - 艺人搜索
**触发时机**: 用户在"艺人名称"输入框输入内容并停止输入1.5秒后

**参数**:
```javascript
{
  search_term: '周杰伦',            // 搜索词（艺人名）
  search_type: 'artist'             // 搜索类型
}
```

**用途**:
- 统计各艺人的搜索热度
- 发现热门艺人趋势
- 优化艺人推荐功能

---

### 6. `select_promotion` - 点击热门演唱会
**触发时机**: 用户点击首页轮播图中的热门演唱会卡片

**参数**:
```javascript
{
  promotion_name: '热门演唱会',     // 推广位名称
  artist_name: '周杰伦',            // 艺人名称
  concert_city: '上海',             // 演唱会城市
  venue_name: '鸟巢',               // 场馆名称
  concert_date: '2026-08-01'        // 演唱会日期
}
```

**用途**:
- 统计推荐位点击率
- 分析哪些演唱会更受欢迎
- 优化首页推荐内容

---

## 🗺️ 用户交互事件

### 7. `location_detect` - 定位功能使用
**触发时机**: 
- 用户点击定位按钮，定位成功或失败时

**参数（成功）**:
```javascript
{
  status: 'success',                // 状态
  city: '北京',                     // 定位到的城市
  latitude: '39.904200',            // 纬度
  longitude: '116.407396'           // 经度
}
```

**参数（失败）**:
```javascript
{
  status: 'error',                  // 状态
  error_message: '定位解析失败'     // 错误信息
}
```

**参数（权限拒绝）**:
```javascript
{
  status: 'permission_denied',      // 状态
  error_code: 1,                    // 错误代码
  error_message: '定位权限被拒绝'   // 错误信息
}
```

**用途**:
- 统计定位功能使用率
- 分析定位成功率
- 优化定位体验

---

## 📈 在 GA4 中查看数据

### 实时报告
进入 GA4 后台 → **报告** → **实时** 查看当前在线用户和实时事件

### 事件报告
进入 GA4 后台 → **报告** → **参与度** → **事件** 查看所有事件的触发次数

### 自定义报告（重点推荐）

#### 报告1：艺人热度排行榜
1. 进入 **探索** → 创建新的探索
2. 维度：添加自定义维度 `artist_name`
3. 指标：
   - `search` 事件计数（搜索次数）
   - `trip_generate` 事件计数（行程生成次数）
   - `purchase` 事件计数（订单数）
4. 按 `artist_name` 分组，降序排序

**结果**: 看到各艺人的热度排名和转化效果

---

#### 报告2：转化漏斗分析
1. 进入 **探索** → 选择 **漏斗分析**
2. 步骤设置：
   - 步骤1: `trip_generate`（生成行程）
   - 步骤2: `select_item`（选择商品）
   - 步骤3: `begin_checkout`（点击购买）
   - 步骤4: `purchase`（完成订单）
3. 查看每一步的转化率和流失率

**结果**: 发现用户在哪个环节流失最多

---

#### 报告3：热门城市路线
1. 进入 **探索** → 创建新的探索
2. 维度：添加 `depart_city` 和 `arrival_city`
3. 指标：`trip_generate` 事件计数
4. 使用表格形式展示

**结果**: 看到最热门的出发-到达城市组合

---

#### 报告4：商品类型偏好
1. 进入 **探索** → 创建新的探索
2. 维度：`item_type`
3. 指标：
   - `select_item` 事件计数（选择次数）
   - `purchase` 事件计数（购买次数）
4. 使用饼图或柱状图展示

**结果**: 看到用户更喜欢订机票还是高铁/酒店

---

## 🔧 常见问题

### Q1: 如何查看某个艺人的订单转化率？
1. 进入 **探索** → 创建新的探索
2. 添加筛选条件：`artist_name` = "周杰伦"
3. 对比 `trip_generate` 和 `purchase` 事件计数
4. 转化率 = purchase数 / trip_generate数

### Q2: 如何导出数据？
在任何报告页面，点击右上角的"导出"按钮，可导出为 Google Sheets、CSV 或 PDF

### Q3: 数据多久更新一次？
- **实时报告**: 几秒内更新
- **标准报告**: 24-48小时更新

### Q4: 如何设置数据保留期限？
进入 **管理** → **数据设置** → **数据保留**，免费版默认保留2个月，可升级到14个月

---

## 📝 维护说明

### 添加新的埋点事件
1. 在关键位置添加代码：
```javascript
if (typeof gtag !== 'undefined') {
  gtag('event', '事件名称', {
    参数1: '值1',
    参数2: '值2'
  });
}
```

2. 更新本文档，记录事件名称、触发时机、参数说明和用途

### 修改现有事件
1. 在代码中修改埋点代码
2. 同步更新本文档
3. 通知数据分析人员，避免历史数据解读错误

---

## 📞 联系方式

如有埋点相关问题，请联系开发团队或参考 [GA4 官方文档](https://support.google.com/analytics/answer/9267735)
